//program to calculate the area of a right angled triangle where the sides are already given

#include <iostream>

using namespace std

int main()
{
  int height, base;
  double area;
  
  height=4,base=5;
  
  area=(base*height)/2;
  
  cout<<"Area of a triangle is "<<area;
  
  return 0;
  
}
